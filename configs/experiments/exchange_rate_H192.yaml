# @package _global_
# Overrides train.yaml for exchange_rate_H192
defaults:
  - override /datasets: exchange_rate

T:      336
H:      192
run_name: exchange_rate_H192
models:
  lambda1: 0.05
trainers:
  lr: 5.0e-4
  early_stopping_patience: 15
  lr_patience: 7
