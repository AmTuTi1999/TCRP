# @package _global_
# Overrides train.yaml for etth1_H96
defaults:
  - override /datasets: etth1
  - override /models: lstm
  - override /trainers: baseline_trainer

T: 336
H: 96
run_name: etth1_baseline
models:
  L: 30
  stride: 1
include_gbm: true
